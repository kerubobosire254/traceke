"""
core/compat.py
--------------
Previously handled TensorFlow/tf-keras compatibility.
Now a no-op — we use PyTorch (facenet-pytorch) instead of TensorFlow.
Kept so existing imports don't break.
"""
# Nothing needed — PyTorch has no compatibility shims required
